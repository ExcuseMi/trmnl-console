{% if data.content %}
    {% assign error = "TRMNL did not run the transform function required to render the plugin." %}
{% endif %}
{% if error %}
    {% render "error_info", error: error, trmnl: trmnl %}
{% else %}
    {% render "content", data: data, trmnl: trmnl %}
{% endif %}